/**
 * Use this file to perform any initialization or configuration for your lynk instance.
 *
 * This file is imported into app.tsx; depending on whether `wait` is set to `true`, your lynk instance will either
 * wait for the `main` function to complete before rendering or not.
 */

export async function main(): Promise<void> {}

export const wait = false;
